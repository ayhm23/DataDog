# Copy to .env and adjust if needed.
# If port 8080 is already in use on your machine, see the root README's
# note on WS_HOST_PORT — update VITE_WS_URL to match whatever host port
# you mapped websocket-gateway to.
VITE_WS_URL=ws://localhost:8080/ws
VITE_QUERY_URL=http://localhost:8002
